const ROLES = {
  ADMIN: 'admin',
  NURSE: 'nurse',
  DOCTOR: 'doctor',
};

const APP_LANGUAGES = ['ru', 'kk', 'en'];
const DEFAULT_LOCALE = 'ru';

module.exports = {
  ROLES,
  APP_LANGUAGES,
  DEFAULT_LOCALE,
};
