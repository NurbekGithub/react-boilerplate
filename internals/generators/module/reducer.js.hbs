/*
*
* {{ properCase defaultName }} reducer
*
*/
export const initialState = {};

/* eslint-disable default-case, no-param-reassign */
function {{ camelCase defaultName }}Reducer(state = initialState, action) {
switch (action.type) {
default: return state;
}
}

export default {{ camelCase defaultName }}Reducer;